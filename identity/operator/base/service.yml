apiVersion: v1
kind: Service
metadata:
  name: keycloak-operator
  labels:
    app.kubernetes.io/name: keycloak-operator
spec:
  ports:
    - name: web
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app.kubernetes.io/name: keycloak-operator
  type: ClusterIP